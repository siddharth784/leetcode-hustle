don't forget else after if, in cases when you need to use it ofc. 
it's a silly mistake. 

e.g.
	if(size==0){
          Node last=new Node();
          last.data=val;
          last.next=null;
          head=tail=last;
          size++;
      }
      else{    			 //don't forget else 
      Node temp=head;
      Node last=new Node();
      last.data=val;
      last.next=null;
      while(temp.next!=null)
        temp=temp.next;
      temp.next=last;
      tail=last;
      size++;
      }
    }