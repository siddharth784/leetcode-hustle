class Solution {
    public int[] replaceElements(int[] arr) {
        int maxr=-1;
        for(int i=arr.length-1;i>=0;i--){
            int temp=Math.max(maxr,arr[i]);
            arr[i]=maxr;
            maxr=temp;
        }
        return arr;
    }
}